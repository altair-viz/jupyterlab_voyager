language: node_js
node_js:
  - "node"
install:
  - pip install jupyterlab==0.28.0
  - jupyter serverextension enable --py jupyterlab --sys-prefix
  - yarn install
  - jupyter labextension link .
  - npm install -g cypress@1.0.3
  - cypress verify
before_script:
  - yarn watch &
  - jupyter lab --port=8889 --watch &
  - sleep 20
script:
  - cypress run --record

